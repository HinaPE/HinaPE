schemaVersion: 2.2.0
attributes:
  space:
    instanceType: extra large
    editor:
      type: Clion

components:
  - name: image-build
    image:
      imageName: myapp:latest
      dockerfile:
        buildContext: docker
        uri: docker/Dockerfile