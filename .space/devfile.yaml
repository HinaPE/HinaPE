schemaVersion: 2.2.0
attributes:
  space:
    instanceType: large
    editor:
      type: Clion

components:
  - name: image-build
    image:
      imageName: myapp:latest
      dockerfile:
        buildContext: docker
        uri: .space/Dockerfile